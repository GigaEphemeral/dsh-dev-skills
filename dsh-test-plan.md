# DSH 开发测试方案（dsh-test-plan）v1

> 版本：v1（2026-09-18，由 `测试方案.md` 改名并特化）｜适用：DSH 生态开发验证
> 测试对象四类：**DSH 本体 / 插件（bundle）/ skill / agent 预设（preset）**
> 位置：本文件 + `tools/keyword-gate.py`（skill 结构门禁）+ 记录文档（结果落盘）

## 1. 目标与门禁

- **目标**：验证 DSH 生态产物可独立使用、输出符合其验收口径。
- **门禁（阶段退出标准）**：每个被测对象至少一次真实小任务试跑通过；结构门禁 PASS；测试结果落盘记录文档。
- **核心约束（token 纪律，D-08）**：一次测试只加载一个被测对象；每个测试单元只加载被测对象**一次**；单次测试总消耗 ≤10,000 tokens。

## 2. 测试对象与验收要点

| 对象 | 是什么 | 测什么 | 典型验证 |
|---|---|---|---|
| **DSH 本体** | Harness 核心（配置/机制） | 行为是否符合预期 | 工具/服务/事件/槽位是否按契约工作；配置组合 |
| **插件（bundle）** | cordis.patch.yml + host/client 代码 | 挂载即用、工具注册、无副作用泄漏 | `plugin add` → 工具可调 → 生命周期正确 → 可卸载 |
| **skill** | SKILL.md 方法论资产 | 触发正确、输出符合验收口径、token 受控 | 真实小任务试跑 + `keyword-gate.py` |
| **agent 预设（preset）** | agent.cordis.yml + preset.yml | 组合正确、切换生效、隔离 | 会话选中预设 → 工具/人格生效 → 普通会话不受污染 |

## 3. 核心原则

1. **一次只测一个对象**：任何测试单元（对话/子代理）只加载一个被测对象。
2. **"只加载一次"**：DSH 的注入（skill/preset）是**一次性注入、无卸载机制**；因此每个测试单元独立上下文（subagent 或新会话），被测对象只注入该单元一次；主会话只调度不加载。
3. **真实任务非空跑**：每个对象配最小真实任务，任务可验证（有可判断产出）。
4. **结果结构化**：通过/不通过 + 问题清单 + 证据，落盘记录文档。
5. **预算控制**：单测试单元预算 ≥5,000 tokens（实测含系统上下文）。

## 4. 测试模式（通用，A 推荐）

**通用测试执行模式详见 `R5-ts-test-designer`（测试执行模式章节）**：A subagent 隔离（一次加载一次）/ B 主会话顺序 / C 新会话回归。本方案直接采用模式 A。

## 5. 测试任务模板（按对象）

### 5.1 插件（bundle）测试
- 任务：装进隔离 profile → 调每个工具（含边界输入）→ 停用/卸载 → 确认无副作用残留。
- 验收：工具全部可调；边界输入正确处理；卸载后无残留行/无报错。

### 5.2 skill 测试
- 任务：每 skill 一个最小真实任务（见下表）。
- 验收：frontmatter 完整 + 必含章节 + 关键词无越界（`keyword-gate.py`）+ 试跑输出符合验收口径。

| skill | 测试任务（示例） | 验收口径 |
|---|---|---|
| gates | 给一个阶段产物判能否进入下一阶段 | 门禁报告：结论/证据/未决项 |
| evidence-chain | 给一段"已完成"描述要求附证据 | 证据四形态之一；未验证项披露 |
| token-budget | 给会话路径做阶段结算 | 跑 token-ledger；对比预算；记录 |
| R1-brainstormer | 一句话需求扩展边界 | 追问≥3 轮；建议联网调研；假设标注⚠️ |
| R2-spec-owner | 收敛发散报告 | Spec 含验收基线+边界输入+DoD |
| R3-pm | 两方案选型 | 决策四段记录 |
| R4-ts-architect | 小 Spec 出 TS 架构 | 架构文档结构齐；核心纯函数化 |
| R5-ts-test-designer | 出 TS 测试方案 | golden case+分层+边界输入 |
| R6-ts-developer | 实现 TS 小函数 | 单测绿+产物四铁律+证据 |
| R7-qa | 回归实现+方案 | 测试报告闭环+结论 |
| R8-reviewer | 一段 diff 双轴审查 | 双轴报告+分级清单 |
| R9-recapper | 工作记录复盘 | 知识条目分层+紧凑输出 |
| R10-budget-auditor | 阶段结算预算 | 结算+缓存命中率+记录 |

### 5.3 agent 预设（preset）测试
- 任务：会话选中预设 → 验证人格/工具生效 → 切回普通预设 → 确认隔离。
- 验收：预设组合正确（工具/人格/私有 skill）；切换生效；普通会话零污染。

## 6. 执行流程（模式 A）

1. 跑结构门禁（skill：`keyword-gate.py`；插件：`plugin_check`/`--dump-config`）。
2. 逐个对象：构造 subagent 提示词 = 被测对象定义（一次性注入）+ 测试任务 + 验收口径 + 预算提示。
3. subagent 执行，返回结构化结果。
4. 主会话核对验收口径；问题清单分级（P0/P1/P2）。
5. 结果落盘记录文档（秒级时间戳）：对象/结论/问题/证据/token。
6. 全部通过 → 阶段门禁过；任一不通过 → 修 → 重测（≤3 轮）。

## 7. 验收与记录

- 结论状态：通过 / 带条件通过（P1 记录）/ 不通过（P0 阻断）。
- 记录位置：`这份工作流-整体开发流程记录.md`（追加，秒级时间戳）+ 隔离测试环境 `test-env/`。
- 证据链要求（evidence-chain）：每个"通过"附任务产出摘要/证据位置。

## 8. 隔离测试环境规则（D-10）

**通用隔离测试环境规则详见 `R6-ts-developer`（隔离测试环境规则章节）**；本方案落于 `test-env/`：
- 环境 `1skillCode/test-env/`（独立隔离目录，主工作区零污染）。
- 数据不清理，由用户确认后才动；每次试跑 `test-env/runs/<对象>/`（task+snapshot+result+artifact）。
- 自测 `test-env/selfcheck/`；测试后 present 暴露给用户查看。
- 编码纪律：D-09 UTF-8（python `utf-8`、pwsh 避免 Out-File BOM）。

## 9. 参数化点（可配置）

| 参数 | 默认 | 说明 |
|---|---|---|
| 单测试单元预算 | 5,000 tokens | 实测含系统上下文 |
| 重测轮次上限 | 3 | 不通过→修→重测 |
| 测试模式 | A（subagent 隔离） | B/C 按需 |
| 并行度 | 串行 | 省 token；可改并行 |
| 测试环境 | `1skillCode/test-env/` | 隔离、不清理、暴露确认 |
