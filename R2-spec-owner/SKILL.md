---
name: spec-owner
description: 需求收敛管理：把发散结果收敛为可验收规格。当需要把形容词变成可验收指标（快→<N ms；稳→可用性%）、定义 In/Out、列假设、产出带验收基线+边界输入清单的 Spec/DoD 时触发。关键词：需求收敛、规格、Spec、验收基线、边界输入、DoD、In/Out。
metadata:
  author: software-workflow
  version: "2.0"
---

# Spec Owner — 需求收敛管理

把「说了什么」变成「怎么算做完」。**不改范围、不替 PM 决策。**

## 触发场景
- 发散报告就绪，需收敛成可验收规格
- 模糊形容词需转可验收指标
- 需定义 In/Out 与验收基线、DoD

## 工作流程
1. **形容词→指标**：快→<N ms/≤N 步；稳→可用性≥99.x%；好用→步数/热区命中率；安全→无高危+权限全覆盖。
2. **定 In/Out**：In=本次必做（写清）；Out=明确不做（写清防蔓延）。
3. **列假设**：每条标 ⚠️；假设不成立则规格失效，需 PM 裁决。
4. **架构师审核技术可行性（对技术难度项）**：凡涉及技术方案实现/技术难度的需求（承接 R1 的 🔧 项），在收敛为可验收规格前，引入架构师（技术负责人）审核：
   - **建议**：该需求的技术可行性结论、候选技术方向、需要的前置调研
   - **搜索**：必要时联网搜索业界成熟实现/主流做法/已知坑，避免拍脑袋定指标
   - **审核**：验收指标是否技术可达（如"毫秒级搜索"是否现实）、是否需拆分/降级、技术风险标注
   - **产出**：技术可行性结论写入 Spec 的 假设/风险/待决问题 三处，供 PM 决策与 R4 后续承接
5. **产出 Spec**：背景/In-Out/功能描述/验收基线（可测）/边界输入清单（多空格/Tab/全角/空值/超限/乱序）/DoD 清单/假设（含 🔧 项技术可行性结论）。

## 边界纪律
- 验收标准**必须含边界输入**，不只测正常输入（教训：`-p` flag 边界翻车）。
- 不替 PM 改范围；范围争议转 R3 决策。
- 每功能点都要"可测验收口径"，不可测=未完成。
- Spec 经 PM 批准后实现侧不得擅改；改走决策记录。
- 🔧 技术难度项未经架构师可行性审核不得定稿验收基线（避免把"做不到"写进 DoD）。
